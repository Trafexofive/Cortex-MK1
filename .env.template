# ==============================================================================
# PROJECT: CORTEX-PRIME - SOVEREIGN CONFIGURATION MANIFEST v1.5
#
# INSTRUCTIONS:
# 1. Copy this file to '.env' before starting the stack: `cp .env.template .env`
# 2. Review and fill in the required values, especially passwords and API keys.
# ==============================================================================

# --- Core Project Identity ---
# This is used to name containers, networks, and volumes to avoid collisions.
PROJECT_NAME=cortex-prime

# --- External Port Mappings ---
# These variables map ports from your HOST machine to the CONTAINER.
# Only change these if you have a port conflict on your host system.
GATEWAY_HOST_PORT=8080
WEB_CLIENT_HOST_PORT=8889
WEB_CLIENT_HOST_PORT=8889
CHIMERA_CORE_HOST_PORT=8001

NEO4J_HTTP_HOST_PORT=7474
NEO4J_BOLT_HOST_PORT=7687
REDIS_HOST_PORT=6380

# --- Database Credentials ---
# These are the credentials for the Neo4j database.
# IMPORTANT: Use a strong, unique password.
NEO4J_USER=neo4j
NEO4J_PASSWORD=<your_secure_password_here>

# --- Internal Service URIs ---
# These URIs are used for communication BETWEEN services inside the Docker network.
# Use the service names (e.g., 'neo4j', 'redis') as the hostname.
# Do NOT use 'localhost' here.
NEO4J_URI=neo4j://neo4j:7687
REDIS_URL=redis://redis:6379

# --- LLM Provider API Keys ---
# API keys for the language model providers used by the Chimera Core.
GEMINI_API_KEY=<your_google_ai_gemini_api_key>
GROQ_API_KEY=<your_groq_api_key>

# --- General Configuration ---
# Set the desired logging level for the application.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
