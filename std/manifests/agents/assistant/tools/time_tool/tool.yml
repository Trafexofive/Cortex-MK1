# ==============================================================================
#           LOCAL TOOL MANIFEST (v1.0 - Sovereign Core Standard)
# ==============================================================================
# TEST: Local Tool - Time Tool (part of assistant agent)

kind: Tool
version: "1.0"
name: "time_tool"
summary: "Get current date and time"
author: "TEST_SUITE"
state: "stable"

description: "Simple time tool that returns current date and time. This is a local tool within the assistant agent."

returns: "Object with success boolean and time/date/datetime strings"

implementation:
  type: "script"
  runtime: "python3"
  entrypoint: "./scripts/time.py"

parameters:
  - name: "operation"
    type: "string"
    description: "Time operation to perform"
    required: true

tags:
  - "time"
  - "datetime"
  - "utility"
