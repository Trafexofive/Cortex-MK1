VoiceTools:
  VoicePipeline:
    name: "voice_pipeline_service"
    description: "A high-performance tool for real-time, streaming voice-to-text (transcription) and text-to-voice (synthesis)."
    runtime: "python3"
    path: "tools/voice_pipeline/run.py"
    parameters_schema:
      type: "object"
      properties:
        operation: { "type": "string", "enum": ["transcribe", "synthesize"] }
        text_input: { "type": "string" }
        # Other params like language, voice model can be added
